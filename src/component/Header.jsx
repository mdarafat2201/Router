import React from "react";

const Header = () => {
  return (
    <div
      style={{
        background: "red",
        color: "#fff",
        textAlign: "center",
        padding: "10px 0",
      }}
    >
      Header
    </div>
  );
};

export default Header;
